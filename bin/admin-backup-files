#!/bin/sh
#
# Performs a filesystem backup for configured directories.
#
# Syntax: admin-backup-files [-v]
#
# Options:
#   -v  Print verbose output.
#
# Files:
#   /etc/admin-backup-files.conf
#

# Configuration variables
CONFIG=/etc/admin-backup-files.conf
LOCALDIR=`dirname $0`/..
HOST=`hostname --fqdn`
DATE_MINUTE=`date '+%Y-%m-%d-%H%M'`
DATE_DAILY=`date '+%Y-%m-%d'`
DATE_MONTHLY=`date '+%Y-%m'`
SNAP_DIR=/backup/$HOST/current
HIST_DIR=/backup/$HOST/history
HIST_DIR_MINUTE=$HIST_DIR/$DATE_MINUTE
HIST_DIR_DAILY=$HIST_DIR/$DATE_DAILY
HIST_DIR_MONTHLY=$HIST_DIR/$DATE_MONTHLY
OPTIONS="--archive --delete --force --ignore-errors --quiet"
VERBOSE="false"

# Function for printing verbose information
printinfo() {
    if [ $VERBOSE = "true" ] ; then
        echo `date +"%F %T"`: $*
    fi
}

# Check for root user
if [ `whoami` != 'root' ] ; then
    echo "ERROR: Only root is allowed to run admin-backup-files" >&2
    exit 1
fi

# Load config to stdin
if [ -f $CONFIG ] ; then
    exec < $CONFIG
elif [ -f $LOCALDIR/$CONFIG ] ; then
    exec < $LOCALDIR/$CONFIG
else
    echo "ERROR: Missing config file $CONFIG"
    exit 1
fi

# Parse command-line arguments
if [ "$1" = "-v" ] ; then
    shift
    VERBOSE=true
fi

# Perform backup
printinfo init backup on $HOST
mkdir -p $SNAP_DIR $HIST_DIR_MINUTE
while read SRC ; do
    printinfo syncing dir $SRC
    mkdir -p $SNAP_DIR$SRC
    rsync $OPTIONS --backup --backup-dir=$HIST_DIR_MINUTE$SRC $SRC/ $SNAP_DIR$SRC/
done

# Remove empty dirs
find $HIST_DIR_MINUTE -depth -type d -exec rmdir {} \; 2> /dev/null

# Update daily and montly dirs
if [ -d $HIST_DIR_MINUTE ] ; then 
    printinfo updating daily and monthly diff dirs
    mkdir -p $HIST_DIR_DAILY $HIST_DIR_MONTHLY
    cp -fluR $HIST_DIR_MINUTE/* $HIST_DIR_DAILY
    cp -fluR $HIST_DIR_DAILY/* $HIST_DIR_MONTHLY
fi

# Remove outdated hourly and daily dirs
find $HIST_DIR -maxdepth 1 -type d -name "????-??-??-????" -mtime +2 -exec rm -rf {} \;
find $HIST_DIR -maxdepth 1 -type d -name "????-??-??" -mtime +30 -exec rm -rf {} \;

# Finished backup
/usr/local/bin/admin-freemem
printinfo done backup on $HOST
